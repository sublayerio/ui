import { textFormatter as json } from '../json'
import { textFormatter as date } from '../date'
import { textFormatter as singleSelect } from '../single-select'
import { textFormatter as relationship } from '../relationship'

export default {
    json,
    date,
    singleSelect,
    relationship
}